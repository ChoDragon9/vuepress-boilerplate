{
  "name": "vuepress-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "dev": "docs"
  },
  "dependencies": {},
  "devDependencies": {
    "@vue/composition-api": "^0.5.0",
    "@vuepress/plugin-google-analytics": "^1.3.1",
    "typescript": "^3.8.3",
    "vuepress": "^1.3.1",
    "vuepress-plugin-clean-urls": "^1.1.1",
    "vuepress-plugin-typescript": "^0.2.0"
  },
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "release": "rm -rf assets src && cp -a docs/.vuepress/dist/. .",
    "push": "npm run build && npm run release && git add -A && git commit -m \"설명 수정\" && git push origin master"
  }
}
